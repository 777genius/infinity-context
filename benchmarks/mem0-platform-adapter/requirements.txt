# Deprecated compatibility list. scripts/bootstrap.sh uses runtime-lock.json for the frozen graph.
# The Mem0 identity is intentionally absent and comes only from runtime-pin.json.
fastapi==0.140.13
httpx==0.28.1
pydantic==2.13.4
uvicorn==0.52.0
